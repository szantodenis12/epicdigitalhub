import { ServicesHub, servicesHubMetadata } from "../../../_pages/services";

export const metadata = servicesHubMetadata("ro");

export default function Page() {
  return <ServicesHub locale="ro" />;
}
